{
    "service": {
        "id": "rest-api",
        "name": "rest-api",
        "tags": ["rest"],
        "port": 8080,
        "checks": [{
            "name": "HTTP rest-api check",
            "http": "http://rest-api.service.consul:8080/healthz",
            "method": "GET",
            "interval": "10s",
            "timeout": "1s"
        }]
    }
}